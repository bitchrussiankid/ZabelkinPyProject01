Напишите функцию count_lines(filename), которая:

Принимает имя файла в качестве аргумента

Открывает файл для чтения с использованием контекстного менеджера with

Подсчитывает количество строк в файле

Возвращает число строк

Обрабатывает случай, когда файл не существует (возвращает -1 или выводит сообщение)

Дополнительно: Напишите основную программу, которая:

Вызывает функцию для файла example.txt

Выводит результат в формате: "File 'example.txt' has X lines"

Требования:

Используйте try-except для обработки FileNotFoundError

Функция должна закрывать файл автоматически

Не используйте глобальные переменные