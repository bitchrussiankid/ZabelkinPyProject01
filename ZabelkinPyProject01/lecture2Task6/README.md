У вас есть строка message = " Hello, World! ".

- Используя базовые методы строк, уберите пробелы в начале и в конце этой строки.

- Затем преобразуйте все символы строки к нижнему регистру.

- Сохраните результат в переменную clean_message.

- Выведите значение переменной clean_message на экран.