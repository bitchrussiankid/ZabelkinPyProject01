Напишите программу, которая:

1. Объявляет три переменные разных типов:

- Целое число (например, возраст)

- Дробное число (например, рост в метрах)

- Строку (например, имя)

2. Выводит значения этих переменных в одной строке через запятую