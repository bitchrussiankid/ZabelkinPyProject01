Напишите программу, которая:

1. Запрашивает у пользователя целое число (например, «Введите год:»)

2. Запрашивает строку (например, «Введите название месяца:»)

3. Преобразует число в строку и объединяет с введенной строкой

4. Выводит результат в формате: «Событие: [строка] [число]»