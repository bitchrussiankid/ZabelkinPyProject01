У вас есть переменная count, которая хранит значение 10.

- Выведите значение переменной count на экран.

- Переопределите переменную count, увеличив ее значение на 5.

- Снова выведите новое значение переменной count на экран.